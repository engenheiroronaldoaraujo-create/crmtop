import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.atendatop.crm",
  appName: "AtendaTop CRM",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
}

export default config
