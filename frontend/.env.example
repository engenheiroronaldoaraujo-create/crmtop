# Supabase project URL and public anon key.
# These are safe to expose in the browser. NEVER put the service role key here.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-anon-key>
